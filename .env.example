# Copy this file to .env and fill in real values. The .env file is gitignored.
#
# Generate strong secret keys:
#   python3 -c "import secrets; print(secrets.token_urlsafe(48))"
# Generate a Fernet key (if/when needed for Airflow connections encryption):
#   python3 -c "import base64,os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"

POSTGRES_PASSWORD=change-me
AIRFLOW_WEBSERVER_SECRET_KEY=change-me
SUPERSET_SECRET_KEY=change-me
SUPERSET_ADMIN_PASSWORD=change-me
